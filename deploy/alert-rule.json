{
  "alert": "RunawayToolLoop",
  "alertType": "TRACES_BASED_ALERT",
  "condition": {
    "compositeQuery": {
      "queries": [
        {
          "name": "A",
          "type": "clickhouse_sql",
          "spec": {
            "chQuery": "SELECT count() FROM signoz_spans WHERE name = 'sentinel.agent.quarantined' AND timestamp >= now() - INTERVAL 5 MINUTE"
          }
        }
      ]
    },
    "op": "1",
    "target": "A",
    "matchType": "1",
    "evalWindow": "5m"
  },
  "labels": {
    "sentinel/policy": "runaway-tool-loop",
    "sentinel/detection": "deterministic"
  },
  "annotations": {
    "summary": "Runaway agent quarantine threshold crossed",
    "description": "Sentinel detected >= 8 identical (tool, args_hash) calls within 60s from a single agent and quarantined its credential."
  },
  "notificationChannels": []
}
